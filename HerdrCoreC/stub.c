// HerdrCore is a pure static-library module: the machine code lives in
// HerdrCore.xcframework (built by scripts/build-herdr-core.sh); this unit
// exists only so the target produces a linkable archive for the clang module
// that declares the C ABI in include/HerdrCore.h.
